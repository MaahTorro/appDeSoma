import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'App_hellow_world',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
